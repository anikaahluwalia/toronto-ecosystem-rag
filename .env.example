# Anthropic API key — get one at https://console.anthropic.com/
# If left blank, the demo runs in offline mode (still functional,
# just shows structured retrieval output instead of generated prose).
ANTHROPIC_API_KEY=
